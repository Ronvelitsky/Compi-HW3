void main() {
  int x = 200b;
  byte y = (byte)300;
  x = (int)y;
  y = (byte) x;
  x = 100b;
  
  bool w = (x < y);
  if((bool)w) {
    int z = x;
    z = y;
    y = (byte)z;
    // z = (int)w;
  }
}